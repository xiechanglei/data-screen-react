import styled from "styled-px2vw-plugin"
import {mainColor} from "@/config";
export const WeatherDayText = styled.div`
    font-size: 14px;
    color: #ffffff;
    line-height: 14px;
`
export const WeatherDayIcon = styled.div`
    margin-top: 18px;
    width: 30px;
`

export const WeatherDayTemperature = styled.div`
    font-size: 14px;
    font-weight: normal;
    color: ${mainColor};
    line-height: 14px;
    margin-top: 10px;
`