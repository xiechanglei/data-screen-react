import styled from 'styled-px2vw-plugin'
import { Div } from '../../present'

export  const DateChartContainer = styled(Div)`
    font-size: 24px;
    margin-right: 20px;
    flex-grow:1;
    font-weight: 700;
`